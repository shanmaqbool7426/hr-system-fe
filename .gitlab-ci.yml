stages:
  - deploy
deploy_dev-fe:
  stage: deploy
  tags:
    - zaffre
  cache:
    key: development
    paths:
      - node_modules/
      - .next/cache/
  script:
    - cp .env.dev .env
    - yarn
    - yarn build
    - pm2 restart devfe
  only:
    - development